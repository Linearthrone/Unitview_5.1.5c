# PROP-2.5 — SLOP-01 → PM-01

**Status:** done (read-only)

Leftovers were deleted, not merged onto live `components/`. `handleSelectLayout` remains as an unused in-memory helper (no hard reload). Historical 5.0.1 notes untouched.
