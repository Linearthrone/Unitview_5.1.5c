# TASK-20260620-001 — Facility Profile, Prints, Light Inputs, ReportSheet (DEV-01 → PM-01)

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260620-001 |
| **From** | DEV-01 |
| **To** | PM-01 |
| **Date** | 2026-06-20 |
| **Status** | **PASS** — verify + close (P1) |
| **Build** | `cd renderer && npm run build` — **PASS** |

---

## Executive summary

Facility profile admin, login/print branding, light-theme input readability, and ReportSheet staff assignments (current + prior shift with snapshot on save/activate) are **implemented and wired**. No additional code changes required in this pass.

---

## Wiring checklist

| # | Deliverable | Status | Evidence |
|---|-------------|--------|----------|
| 1 | Admin Facility Profile section | **PASS** | `facility-settings-form.tsx` in `admin-dashboard.tsx` — name, address, phone, logo upload/remove |
| 2 | Persist `facility_profile` in SimpleDatabase | **PASS** | `database-simple.ts` (`getFacilityProfile` / `saveFacilityProfile`); `facilityService.ts` |
| 3 | Login screen shows saved name + logo | **PASS** | `auth-container.tsx` loads profile via `getFacilityProfile()`; passes `facilityName` + `logoDataUrl` to `login-screen.tsx` |
| 4 | Charge report print uses `FacilityPrintHeader` | **PASS** | `printable-report.tsx`; `unit-view-client.tsx` passes `facilityProfile` to `<PrintableReport>` |
| 5 | Assignment printouts use `FacilityPrintHeader` | **PASS** | `printable-assignments.tsx`; profile passed from `unit-view-client.tsx` and assignment print dialog |
| 6 | Light theme input/textarea readability | **PASS** | `globals.css` `.theme-light input/textarea/select`; `input.tsx` / `textarea.tsx` use `text-foreground` |
| 7 | ReportSheet current nurse + PCT (live) | **PASS** | `report-sheet.tsx` — `resolveAssignedNurse` / `resolveAssignedTech` from live `nurses` / `techs` |
| 8 | ReportSheet prior shift nurse + PCT UI | **PASS** | `report-sheet.tsx` displays `priorShiftNurse` / `priorShiftTech` |
| 9 | Prior-shift snapshot on Save assignments | **PASS** | `unit-view-client.tsx` `handleSaveAssignments` calls `snapshotPriorShiftStaff()` before archive |
| 10 | Prior-shift snapshot on Activate oncoming shift | **PASS** | `handleActivateOncomingShift` snapshots outgoing shift before promote |
| 11 | Persist prior fields on patient records | **PASS** | `patient-staff-assignments.ts` `snapshotPriorShiftStaff()`; types in `patient.ts`; save path updates patients |

---

## Key files verified

- `renderer/src/types/facility.ts`, `renderer/src/types/patient.ts`
- `renderer/src/services/facilityService.ts`, `assignmentService.ts`
- `renderer/src/lib/patient-staff-assignments.ts`
- `renderer/src/components/facility-settings-form.tsx`, `facility-print-header.tsx`
- `renderer/src/components/auth-container.tsx`, `login-screen.tsx`
- `renderer/src/components/unit-view-client.tsx`, `report-sheet.tsx`
- `renderer/src/globals.css`

---

## Before / after notes

| Area | Before (task issuance) | After (verified) |
|------|------------------------|------------------|
| Login branding | Placeholder only | Saved facility name + logo from DB |
| Print headers | Partial / generic | `FacilityPrintHeader` with saved profile on charge report + assignments |
| Light inputs | Washed-out text risk | Dark `--foreground` on light surfaces + component `text-foreground` |
| Prior shift data | UI empty until wired | Snapshot on save assignments + activate oncoming; persists on patient |

---

## Coordination with TASK-20260430-005

Light-theme input contrast overlaps with 005; token changes in `globals.css` and component classes are consistent — no conflicting regressions observed.

---

## QA queue

Manual QA: admin profile save/reload, logout login branding, both print paths, save assignments → prior shift populated, activate oncoming → prior shift reflects outgoing assignments, reload persistence.
