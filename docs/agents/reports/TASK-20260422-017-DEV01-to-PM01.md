---
type: report
task_id: 017
from: DEV-01
to: PM-01
status: Completed
completed: 2026-04-30 02:56
---

# TASK-20260422-017 DEV-01 Completion Report

## Change List

| # | Action | File | Description |
|---|---|---|---|
| 1 | Added | `renderer/playwright.config.ts` | Added deterministic Playwright config with auto web server startup on `127.0.0.1:4173`. |
| 2 | Added | `renderer/smoke/qa-smoke.spec.ts` | Added smoke test covering startup/login/dashboard/menu-open/oncoming board/basic drag signal. |
| 3 | Added | `renderer/scripts/run-qa-smoke.mjs` | Added one-command runner that executes smoke tests and writes a timestamped log artifact. |
| 4 | Modified | `renderer/package.json` | Added script: `qa:smoke`. |
| 5 | Added | `docs/agents/qa-smoke/README.md` | Added QA run documentation and artifact location guidance. |

## Deterministic one-command run

From `renderer/`:

```bash
npm run qa:smoke
```

This command:
- starts/uses local app server via Playwright webServer config,
- executes smoke checks,
- writes a log artifact to `docs/agents/reports/artifacts/qa-smoke-<timestamp>.log`.

## Smoke coverage implemented

Automated checks included:
1. App startup -> login screen visible (`Staff Login`).
2. Login flow (`1001` / `nurse123`).
3. Enter unit dashboard -> in-unit header visible.
4. Menu action smoke -> `Print` menu opens and `Charge report` option is visible.
5. Oncoming shift board opens/closes.
6. Basic drag/drop interaction signal on patient card (`dragstart` / `dragend` dispatch).

## Evidence artifacts

Successful smoke log:
- `docs/agents/reports/artifacts/qa-smoke-2026-04-30T06-55-36-647Z.log`

## Validation outputs

```text
cd renderer && npx tsc --noEmit
(exit 0)
```

```text
npm run build
> unitview-renderer@5.0.1 build
> tsc && vite build
...
> unitview-windows@5.0.2 build:main
> tsc -p tsconfig.main.json
(exit 0)
```
