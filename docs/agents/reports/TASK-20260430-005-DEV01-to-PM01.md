# TASK-20260430-005 — Theme Text Contrast (DEV-01 → PM-01)

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260430-005 |
| **From** | DEV-01 |
| **To** | PM-01 |
| **Date** | 2026-06-20 |
| **Status** | **PASS** — verify + close |
| **Build** | `cd renderer && npm run build` — **PASS** |
| **Related** | TASK-20260620-001 (overlapping input contrast work) |

---

## Executive summary

Contrast improvements are landed in `globals.css` CSS variables and shadcn input/textarea components. Muted and secondary text tokens are strengthened for both light (`:root`) and dark (`.dark`) themes. Light-theme form surfaces receive explicit foreground color rules.

---

## Token summary (before → after intent)

| Token | Light (`:root`) | Dark (`.dark`) |
|-------|-----------------|----------------|
| `--foreground` | `222 47% 8%` — darker body text | `210 40% 98%` — bright primary text |
| `--muted-foreground` | `215 25% 32%` — darker muted labels | `215 25% 78%` — brighter muted on dark surfaces |
| `--secondary-foreground` | `222 47% 12%` | `210 40% 96%` |
| `--card-foreground` | `222 47% 8%` | `210 40% 98%` |
| `--primary-foreground` | `222 47% 11%` (dark on soft blue primary) | `222 47% 11%` |

**Light-theme form override:** `.theme-light input, textarea, select` → `text-foreground` / `color: hsl(var(--foreground))`.

---

## Component verification

| Surface | File | Result |
|---------|------|--------|
| Input | `renderer/src/components/ui/input.tsx` | **PASS** — `text-foreground`, `placeholder:text-muted-foreground` |
| Textarea | `renderer/src/components/ui/textarea.tsx` | **PASS** — same pattern |
| Global tokens | `renderer/src/globals.css` | **PASS** — `:root` and `.dark` blocks updated |
| Login / admit / admin forms | Uses shared Input/Textarea + theme classes | **PASS** (code review) |
| Dashboard / header / cards | Token-driven `text-foreground`, `text-muted-foreground` | **PASS** (no regressions in build) |

---

## Gaps / follow-ups

| Item | Severity | Notes |
|------|----------|-------|
| Native `<select>` outside `.theme-light` wrapper | Low | Most selects use shadcn `Select` component; global `.theme-light` rule covers class-scoped forms |
| Patient card label contrast on custom colors | Low | Cards use design tokens; no change needed in this pass |
| Placeholder vs input text ratio | Low | Acceptable with updated `--muted-foreground` |

No conflicting edits with TASK-20260422-013 clinical-dark direction observed.

---

## Coordination

Overlapping June 20 input readability work documented in **TASK-20260620-001-DEV01-to-PM01.md** — merged verification, single token source of truth in `globals.css`.

---

## QA suggestion

Visual pass: light theme login, admit dialog, facility settings form; dark theme unit view header and patient cards; confirm muted labels readable without washing out body text.
