# PROP-2.1 — FED-01 → PM-01

**Status:** done

`loadBoardForLayout` replaces hard reloads. `availableLayouts` is stateful. Import / save-as / create-unit stay on the board. Skip-first-autosave is set before swapping state.
