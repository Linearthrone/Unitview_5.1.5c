# PROP-2.2 — FED-01 → PM-01

**Status:** done

Deleted unimported root twins, `App-auth`, debug files, unused `src/ui/`, `better-sqlite3.d.ts`, `use-autosave`, `unit-dashboard`. Live copies remain under `components/`. `tsconfig` exclude list is now only `vault-init.test.ts`. Renderer `tsc` clean.
