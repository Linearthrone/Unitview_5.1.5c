# TASK-20260422-019 -- QA01-to-PM01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260422-019 |
| **From** | QA-01 |
| **To** | PM-01 |
| **Priority** | P2 |
| **Depends on** | TASK-20260422-018 (superseded by TASK-20260430-004) |
| **Related issues** | `ISSUE-20260422-003-unit-map-mouse-wheel-zoom.md`, `ISSUE-20260430-002-unit-map-zoom-controls-and-fit-default.md` |
| **Test date** | 2026-06-20 |
| **Environment** | Windows 10.0.26200; UnitView `5.1.5-c`; build `1a14630`; `C:/Users/kurtw/StudioProjects/UnitView/Unitview_5.1.5c` |
| **Authority** | `TASK-20260422-019-PM01-to-QA01.md`, `TASK-20260430-004-PM01-to-DEV01.md`, `TASK-20260430-004-DEV01-to-PM01.md`, `TASK-20260620-004-PM01-to-ALL.md` (Step 4 note) |

## Executive summary

QA validated unit-map zoom per current product direction: **+ / − magnifying-glass controls** and **fit-to-view default** in `patient-grid.tsx`. Legacy mouse-wheel zoom is intentionally absent (no `onWheel` handler) per TASK-20260430-004 to avoid scroll conflict.

Production build and Playwright smoke both **pass**. Code review confirms zoom bounds, fit algorithm, control affordances, reset-to-fit, resize recalculation, and unchanged drag/drop/selection handler wiring inside the scaled transform. No blocking defects found.

**Final verdict: READY**

---

## Scope note (wheel vs controls)

`TASK-20260422-019` originally targeted mouse-wheel zoom (`ISSUE-20260422-003`). PM sprint guidance (`TASK-20260620-004` Step 4) directs QA to validate the **current +/- control UX** delivered under `TASK-20260430-004` / `ISSUE-20260430-002`, not legacy wheel behavior unless still present. Wheel zoom matrix rows are marked **N/A (superseded)** below.

---

## Environment and build evidence

| Item | Value |
|------|--------|
| OS | Windows 10.0.26200 |
| App version | `5.1.5-c` (`renderer/package.json`) |
| Git commit | `1a14630` |
| Build command | `cd renderer && npm run build` |
| Build exit code | **0** (PASS) -- `tsc && vite build`, 2606 modules, ~4.1s |
| Smoke command | `cd renderer && npm run qa:smoke` |
| Smoke artifact | `docs/agents/reports/artifacts/qa-smoke-2026-06-20T07-55-20-339Z.log` |
| Smoke exit code | **0** (PASS) -- 1 passed (4.5s) |

---

## Validation matrix

### 1. Wheel zoom in/out inside map viewport

| Check | Result | Evidence |
|-------|--------|----------|
| Wheel-up zooms in | **N/A** | Superseded by TASK-20260430-004; no `onWheel` / `wheel` handler in `renderer/src` |
| Wheel-down zooms out | **N/A** | Same as above |
| No scroll/zoom conflict | **PASS** | Viewport uses `overflow-auto`; wheel scrolls pan only (`patient-grid.tsx` L388-391) |

**Area verdict: PASS** (superseded design; scroll conflict avoided)

---

### 2. Zoom bounds respected (min/max)

| Check | Result | Evidence |
|-------|--------|----------|
| Floor enforced | **PASS** | `MIN_ZOOM_FLOOR = 0.2` (20%); `clampZoom()` L113-115; `canZoomOut` disables − at floor L197 |
| Ceiling enforced | **PASS** | `MAX_ZOOM_CEILING = 2` (200%); `canZoomIn` disables + at ceiling L196 |
| Step size | **PASS** | `ZOOM_STEP = 0.08` per click L19, L180-188 |

**Area verdict: PASS**

---

### 3. Reset returns to fit-to-view

| Check | Result | Evidence |
|-------|--------|----------|
| Percentage label shows current zoom | **PASS** | `{Math.round(zoom * 100)}%` L373 |
| Click resets to fit | **PASS** | `resetZoom()` clears `userAdjustedZoomRef` and sets `zoom` to `fitZoom` L190-193 |
| At-fit state disables reset | **PASS** | `atFitZoom` tolerance 0.02; button `disabled={atFitZoom}` L369; title "Showing entire unit" L371 |
| Default on unit load | **PASS** | `applyFit(true)` on init L150-159; resets override on `patients/nurses/techs` change L141-143 |

**Area verdict: PASS**

*Note: Reset targets **fit-to-view** (dynamic %), not fixed 100%, per ISSUE-20260430-002.*

---

### 4. Fit-to-view default and resize behavior

| Check | Result | Evidence |
|-------|--------|----------|
| Fit algorithm | **PASS** | `measureFitZoom()`: `min(availW/contentW, availH/contentH)` with 16px padding, clamped L127-138 |
| Content measurement | **PASS** | `ResizeObserver` on viewport + grid L165-176; `contentSize` drives wrapper dimensions L392-404 |
| Manual override preserved | **PASS** | `zoomIn`/`zoomOut` set `userAdjustedZoomRef.current = true` L181, L186; resize only auto-updates when `!userAdjustedZoomRef.current` L170-171 |
| Control affordance | **PASS** | `ZoomOut` / `ZoomIn` lucide icons L7, L363, L385; `role="group"` + `aria-label="Unit map zoom controls"` L350-351 |

**Area verdict: PASS**

---

### 5. Card drag/drop at low / default / high zoom

| Check | Result | Evidence |
|-------|--------|----------|
| Patient drag handlers | **PASS** | `draggable`, `onDragStart`, `onDragEnd` on patient wrapper L245-247; `data-patient-id` preserved for smoke |
| Cell drop handlers | **PASS** | `onDragOver` / `onDrop` on grid cells L239-240 unchanged inside scaled inner wrapper |
| Nurse/tech drag | **PASS** | Nurse cards `draggable` + `onDragStart` L421-422; tech cards same pattern L439+ |
| Nurse slot drops | **PASS** | `handleDropOnNurseSlot` passed to `NurseAssignmentCard` L332 |
| Smoke drag signal | **PASS** | `qa-smoke.spec.ts` L66-69: `dragstart`/`dragend` on `[data-patient-id]` |
| Drop accuracy at 75% / 150% | **PARTIAL** | Handler wiring correct in code; pixel-accurate drop at extremes not exercised in automated run |

**Area verdict: PASS** (recommend optional manual spot-check at zoom extremes)

---

### 6. Selection / assignment interactions after zoom changes

| Check | Result | Evidence |
|-------|--------|----------|
| Patient selection | **PASS** | `onSelectPatient` passed through `PatientBlock` L261 |
| Staff assignment callbacks | **PASS** | `onAssignStaff`, `onAssignNurse`, `onAssignTech` wired in nurse/tech cards L311-337 |
| Lock/read-only gating | **PASS** | `isEffectivelyLocked` / `isReadOnly` suppress drag and drops L241, L245, L421 |
| Smoke regression | **PASS** | Login, enter unit, print menu, oncoming board, basic drag -- all green in smoke |

**Area verdict: PASS**

---

## Requirements cross-check (TASK-20260430-004)

| # | Requirement | QA result |
|---|-------------|-----------|
| 1 | Zoom out + zoom in buttons (magnifying-glass idiom) | **PASS** |
| 2 | Percentage between buttons; click resets to fit | **PASS** |
| 3 | Default fit on unit load / switch | **PASS** |
| 4 | Recalculate on resize when user has not manually adjusted | **PASS** |
| 5 | Drag/drop at non-default zoom | **PASS** (code); partial manual |
| 6 | No wheel zoom / scroll conflict | **PASS** |

---

## Issues filed

### ISSUE-019-001 -- No automated zoom-button coverage in smoke harness (Info)

| Field | Value |
|-------|--------|
| **Severity** | Info (test gap) |
| **Matrix** | Interaction safety |
| **Repro** | Run `npm run qa:smoke`; observe no clicks on Zoom in/out or percentage reset. |
| **Expected** | Optional smoke step asserting zoom controls visible and percentage changes on +/−. |
| **Actual** | `qa-smoke.spec.ts` covers login/unit entry/drag only. |
| **Blocking** | No |

### ISSUE-019-002 -- Manual drop accuracy at zoom extremes not executed (Info)

| Field | Value |
|-------|--------|
| **Severity** | Info (process gap) |
| **Matrix** | Drag/drop at zoom extremes |
| **Repro** | Enter unit; click Zoom in repeatedly to ~150%; drag patient to empty cell; repeat at min zoom. |
| **Expected** | Drop targets align with visual cells at scaled transform. |
| **Actual** | Code paths verified; hands-on drop accuracy not run in this QA session. |
| **Blocking** | No |

No new blocking `ISSUE-...` files created. `ISSUE-20260430-002` may be closed on PM acceptance of this READY verdict.

---

## QA sign-off

| Item | Status |
|------|--------|
| Pass/fail table with reproducible steps | Done |
| Build green | Done |
| Smoke green | Done |
| Failures logged | Done (2 info items, no blockers) |
| Blocking defects | None |
| **Release recommendation** | **READY** |

**QA-01** -- Unit map +/- zoom controls and fit-to-view default acceptable for release; optional follow-up: smoke zoom steps and manual drop spot-check at 75%/150%.
