# TASK-20260620-003 — Session UX Verification Matrix (DEV-01 → PM-01)

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260620-003 |
| **From** | DEV-01 |
| **To** | PM-01 |
| **Date** | 2026-06-20 |
| **Status** | **PASS** (7/8 PASS, 1 PARTIAL — no critical fixes required) |
| **Build** | `cd renderer && npm run build` — **PASS** |

---

## Verification matrix

| # | Area | Result | Evidence / repro |
|---|------|--------|------------------|
| 1 | **Spectra panel collapse** | **PASS** | `unit-view-client.tsx`: desktop collapsible rail (`isSpectraPanelExpanded`); below `lg`: FAB opens right `Sheet` with full `SpectralinkDeviceTable` (lines ~1437–1465) |
| 2 | **Map horizontal stretch** | **PASS** | `patient-grid.tsx`: viewport `flex-grow`; cells `items-stretch justify-stretch`; patient blocks `w-full h-full` |
| 3 | **Admin tools routing** | **PASS** | `roles.ts` — `Entity Admin` / `Facility Admin` → `isAdmin`; `user-dashboard.tsx` admin button → `auth-container` `admin` view; unit view `showAdminTools={roleCaps.isAdmin}` in `app-header.tsx` |
| 4 | **Facility dashboard** | **PASS** | `user-dashboard.tsx` facility stats section; `admin-dashboard.tsx` embeds `FacilitySettingsForm` |
| 5 | **Staff nurse context menu** | **PASS** | `nurse-assignment-card.tsx` — assign/change staff, clear assignments, remove nurse via context menu |
| 6 | **Nurse assignment icon bug** | **PASS** | Assigned nurses show name + `User` icon; unassigned slots use `UnassignedStaffNameButton` (not conflated with assigned state); patient slots show `Bed N` vs `Assign...` correctly |
| 7 | **Nurse card resize handle** | **PASS** | `nurse-card-resize-handle.tsx` drag adjusts capacity; `unit-view-client.tsx` `handleResizeNurseCapacity` + `resizeNurseAssignedPatientIds`; persists via save layout flow |
| 8 | **Unassigned staff assign button** | **PARTIAL** | All role cards expose assign via clickable `UnassignedStaffNameButton` + context menu (`charge-nurse-card`, `unit-clerk-card`, `nurse-assignment-card`, `patient-care-tech-card`). Explicit **`+ Assign staff member`** body button (`assign-staff-member-button.tsx`) only on oncoming cards in `shift-maker-dialog.tsx`, not on live unit-map cards |

---

## Item 8 detail (PARTIAL)

**Expected (TASK-20260430-007):** Prominent `+ Assign staff member` label on card body when unassigned.

**Actual:** Functional assign affordance via dashed-underline placeholder name (`unassigned-staff-name-button.tsx`) and context-menu items with `UserPlus` icon. Behavior wires to `AssignStaffDialog` correctly; label pattern differs from 007 spec.

**Decision:** Not a critical gap — assign flow works on all card types. No code change in this pass.

---

## Regression notes

- WALLDISPLAY privacy: unchanged (`isReadOnly`, `canSeePatientIdentifiers: false` in unit view).
- Print flows: facility headers unaffected (cross-check with TASK-20260620-001).

---

## QA queue

PM may queue QA on matrix rows 1 (narrow viewport Spectra), 2 (map stretch at varied widths), 7 (resize + save layout), 8 (assign UX acceptance).
