# TASK-20260422-011 — PM01-to-DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260422-011 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P0 |
| **Depends on** | None |
| **Related issue** | `ISSUE-20260422-001-unit-isolation-and-room-dnd.md` |

## Context

User reported three linked defects:

1. All units show the same state (cross-unit contamination).
2. Right-column room cards do not move on drag.
3. A duplicate right-column room appears on first row.

## Objective

Fix unit-level isolation and room-map drag/render defects so unit boards behave independently and consistently.

## Scope

Likely touchpoints (adjust as needed):
- `renderer/src/components/unit-view-client.tsx`
- `renderer/src/components/patient-grid.tsx`
- `renderer/src/services/patientService.ts`
- `renderer/src/services/layoutService.ts`
- `renderer/src/lib/database-simple.ts`
- Related map/room rendering utilities

## Required outcomes

1. **Unit isolation fixed**
   - Loading Unit A and Unit B must read/write different layout-scoped datasets.
   - Editing Unit A must not mutate Unit B.

2. **Right-column drag fixed**
   - Right-column room cards move with drag/drop under unlocked layout mode.
   - Save persists new positions to correct layout.

3. **Duplicate room rendering fixed**
   - No duplicate right-column card appears on first row unless explicitly configured.

## Acceptance criteria

1. Repro before/after documented in DEV report.
2. `cd renderer && npx tsc --noEmit` passes.
3. `npm run build` at repo root passes.
4. Manual validation:
   - Create visible difference in Unit A and Unit B; verify isolation.
   - Drag a right-column room to new cell and verify persisted result after reload.
   - Confirm no duplicate room card remains.

## Completion report

Create: `docs/agents/reports/TASK-20260422-011-DEV01-to-PM01.md`

Include:
- Root cause summary for each of the 3 defects.
- File list changed.
- Typecheck/build output.
- Manual validation notes with exact steps and observed outcomes.
