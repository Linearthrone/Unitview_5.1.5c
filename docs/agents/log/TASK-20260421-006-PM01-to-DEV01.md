# TASK-20260421-006 — PM01 ? DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260421-006 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P1 |
| **Depends on** | TASK-20260421-004 |

## Context

Repo still contains duplicate/legacy renderer trees that increase regression risk (`src/*.tsx` duplicates of `src/components/*`, legacy files, old DB variants).

## Objective

Produce and execute a **safe cleanup pass** for dead/legacy files not used by active app path, while keeping build green.

## Scope

- `renderer/src/` duplicate top-level UI/component files
- known legacy candidates: `*-old`, `*-enhanced`, obsolete backups
- `renderer/tsconfig.json` include/exclude cleanup if needed

## Guardrails

- Do not remove any file still imported by active path (`main.tsx -> App.tsx -> components/auth-container.tsx` and descendants).
- Prefer a two-step approach: (1) inventory + proof unused, (2) delete.

## Acceptance criteria

1. Provide keep/delete matrix in report with import-graph evidence.
2. After cleanup, `cd renderer && npx tsc --noEmit` and `npm run build` (root) both pass.
3. No runtime entrypoint breakages introduced.

## Completion report

Create: `docs/agents/reports/TASK-20260421-006-DEV01-to-PM01.md`

Include:
- Deleted file list.
- Proof of unused status per deleted cluster.
- Post-cleanup build/typecheck outputs.
