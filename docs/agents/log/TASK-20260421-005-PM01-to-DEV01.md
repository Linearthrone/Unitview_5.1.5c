# TASK-20260421-005 — PM01 ? DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260421-005 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P1 |
| **Depends on** | TASK-20260421-004 |

## Context

Renderer has no ESLint config; `npm run lint` currently fails to start. We need a real static-quality gate.

## Objective

Set up ESLint configuration for renderer TS/TSX, fix immediate high-signal issues, and make lint usable in CI/local checks.

## Scope

- `renderer/` lint config files (flat or classic config)
- `renderer/package.json` scripts if needed
- Minimal code edits required to pass lint baseline

## Acceptance criteria

1. `cd renderer && npm run lint` exits `0`.
2. Config includes TypeScript + React hooks + react-refresh rules already in deps.
3. No blanket disabling of whole rule sets; any disable comments must be justified and narrow.

## Completion report

Create: `docs/agents/reports/TASK-20260421-005-DEV01-to-PM01.md`

Include:
- Final lint command output summary.
- List of top 5 categories of fixes made.
