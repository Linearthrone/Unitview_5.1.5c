# TASK-20260421-002 — PM01 → DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260421-002 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P0 |
| **Depends on** | TASK-20260421-001 (types for persisted rows should be stable first) |

## Context

`renderer/src/services/patientService.ts` and `renderer/src/services/nurseService.ts` fail `tsc` with issues including: object literals assigning `layoutName` where types disallow it, wrong arity calls (`Expected N arguments, but got M`), incompatible `Spectra` vs `string`, and unknown property `roomRange` on `PatientCareTech`.

## Objective

Fix **all TypeScript errors** in these two service modules and ensure their public APIs match how `components/unit-view-client.tsx` and related dialogs call them.

## Scope

- `renderer/src/services/patientService.ts`
- `renderer/src/services/nurseService.ts`
- Call sites only if required for type correctness (prefer minimal edits).

## Acceptance criteria

1. `npx tsc --noEmit` from `renderer/` shows **no errors** in `patientService.ts` or `nurseService.ts`.
2. Smoke checklist (DEV runs locally, evidence in report):
   - Load unit → patients/nurses/techs appear.
   - Admit/update patient path does not throw in console.
   - Add staff member path does not throw (or document known follow-up as Issue).

## Completion report

Create: `docs/agents/reports/TASK-20260421-002-DEV01-to-PM01.md` with `tsc` excerpt and brief smoke notes.
