# TASK-20260421-003 — PM01 → DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260421-003 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P0 |
| **Depends on** | TASK-20260421-001, TASK-20260421-002 |

## Context

Remaining `tsc` failures (after 001/002) include:

- `renderer/src/types/forms.ts` — `z.enum()` called with arrays that TypeScript does not treat as non-empty tuples.
- `renderer/src/App.tsx` — `UndoRedoProvider` requires `initialState` prop.
- `renderer/src/hooks/use-autosave.ts` — `NodeJS` namespace not available under current `tsconfig` `types`.
- `renderer/src/components/printable-report.tsx` — invalid `title` prop on Lucide icon component.
- `renderer/src/lib/database.ts` — missing `better-sqlite3` module/types (decide exclude vs. optional dep vs. shim).
- `renderer/src/lib/initial-nurses.ts` — missing required `role` on nurse seed objects.
- `renderer/src/lib/mock-patients.ts` — missing `isInRestraints` on a mock entry.

## Objective

Drive **renderer `npx tsc --noEmit` to zero errors** for the included project set defined in `renderer/tsconfig.json`.

## Scope

- Files listed above only; do not expand scope to duplicate legacy trees under `src/*.tsx` excluded by tsconfig unless PM issues a new task.

## Acceptance criteria

1. From `renderer/`, `npx tsc --noEmit` exits **0**.
2. From repo root, `npm run build` still succeeds (renderer `build:no-check` + main `tsc`).

## Completion report

Create: `docs/agents/reports/TASK-20260421-003-DEV01-to-PM01.md` with:

- Full success line or last 30 lines of `tsc` output.
- Note on `database.ts` resolution (excluded vs. dependency vs. types-only import).

## Follow-up (PM will assign separately)

- Switch root `build:renderer` to typecheck build for release confidence (OPS/PM decision).
