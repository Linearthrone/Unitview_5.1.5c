# TASK-20260422-017 — PM01-to-DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260422-017 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P1 |
| **Depends on** | TASK-20260422-011 |
| **Related issue** | `ISSUE-20260422-002-qa-ui-smoke-not-fully-automatable.md` |

## Objective

Provide a minimal deterministic QA smoke harness for key UI flows so QA can produce evidence-based pass/fail results without manual-only blockers.

## Scope

- Add lightweight scripted smoke checks (Electron or browser automation) for:
  1. app startup + login route
  2. enter unit dashboard
  3. menu actions smoke path (no deep business assertions required)
  4. basic drag/drop interaction signal
- Add docs on how QA runs it and where logs are saved.

## Acceptance criteria

1. QA can run one command and produce a log artifact for smoke run.
2. At least startup/login/dashboard/menu-open checks are automated with deterministic output.
3. `cd renderer && npx tsc --noEmit` passes.
4. `npm run build` at root passes.

## Completion report

Create: `docs/agents/reports/TASK-20260422-017-DEV01-to-PM01.md`
