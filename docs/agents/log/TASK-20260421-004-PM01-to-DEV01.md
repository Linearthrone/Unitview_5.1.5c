# TASK-20260421-004 — PM01 ? DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260421-004 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P0 |
| **Depends on** | None |

## Context

Root build currently bypasses renderer typecheck by using `build:no-check`.
This allows type regressions to ship.

## Objective

Make release build path enforce renderer typecheck and fail fast on type errors.

## Scope

- `package.json` (root)
- `BUILD_INSTRUCTIONS.md` (only update relevant command docs)

## Required change

- Update root `build:renderer` from `cd renderer && npm run build:no-check` to `cd renderer && npm run build`.
- Keep a separate explicit script if you still need non-typechecked local speed path (optional, but name it clearly, e.g. `build:fast`).

## Acceptance criteria

1. `npm run build` (repo root) succeeds when renderer typecheck is clean.
2. Introduce an intentional type error locally (temporary), confirm build fails, then revert the temporary change.
3. Document the final command behavior in `BUILD_INSTRUCTIONS.md`.

## Completion report

Create: `docs/agents/reports/TASK-20260421-004-DEV01-to-PM01.md`

Include:
- Final `npm run build` output excerpt.
- One-line proof that build fails on type errors (what temporary test was used).
