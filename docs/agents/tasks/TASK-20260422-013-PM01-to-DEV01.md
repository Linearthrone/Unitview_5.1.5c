# TASK-20260422-013 — PM01-to-DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260422-013 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P1 |
| **Depends on** | TASK-20260422-011 |

## User-approved design direction

- Main UI redesign scope: **C (Full redesign)**
- Dark theme style: **C (Clinical dark)**
- Color personality: **B (Neutral professional)**
- Priority: **C (Visual polish first)**

## Objective

Deliver a full redesign of the main UI screen and dark theme system, preserving functionality while significantly improving visual quality and consistency.

## Scope

Likely files (adjust as needed):
- `renderer/src/components/user-dashboard.tsx`
- `renderer/src/components/app-header.tsx`
- `renderer/src/components/unit-view-client.tsx`
- `renderer/src/globals.css`
- `renderer/tailwind.config.ts`
- shared UI tokens/components under `renderer/src/components/ui/*`

## Design requirements

1. **Main screen redesign (full)**
   - Rework layout hierarchy (header, stats, action zones, core workspace).
   - Improve spacing rhythm, card composition, and information grouping.
   - Make primary actions visually prominent and secondary actions quieter.

2. **Dark theme redesign**
   - Build a polished clinical-dark foundation (blue-gray neutrals, controlled contrast).
   - Keep neutral-professional accents (avoid overly saturated neon tones).
   - Ensure text, icons, borders, states, and charts/tables remain legible.

3. **Polish-first details**
   - Refined shadows, borders, radius, and hover/focus/pressed states.
   - Consistent typography scale and weight usage.
   - Remove visually noisy elements where possible.

## Non-negotiables

- Do not break existing workflows or data behavior.
- No functional regressions in patient/staff/unit operations.
- Keep accessibility at least current or better (contrast and focus visibility).

## Acceptance criteria

1. Visual redesign is clearly noticeable and cohesive in dark mode.
2. Main user screen and in-unit header/workspace look modern and consistent.
3. `cd renderer && npx tsc --noEmit` passes.
4. `cd renderer && npm run lint` passes.
5. `npm run build` at repo root passes.

## Completion report

Create: `docs/agents/reports/TASK-20260422-013-DEV01-to-PM01.md`

Include:
- Before/after screenshots list (paths or capture references).
- Token/palette summary for dark theme.
- Build/typecheck/lint outputs.
- Notes on any intentional UX trade-offs.
