# TASK-20260620-003 — PM01-to-DEV01

## PM CONTROL STATUS

**STATUS: COMPLETE (DEV report accepted 2026-06-20 — 7 PASS / 1 PARTIAL; QA Step 1 in 012 queue)**

Session items may already be implemented in renderer (untracked/local). **Do not re-implement** unless verification fails. Deliver evidence in DEV report; PM will queue QA if needed.

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260620-003 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P2 |
| **Version** | 5.1.5-c |
| **Related tasks** | `TASK-20260430-008`, `TASK-20260430-004`, `TASK-20260430-007`, `TASK-20260422-013` |

## Summary

**Verify + QA** recent session UX fixes that may already be coded. Fix only gaps found during verification; otherwise document PASS with screenshots/steps.

## Scope checklist (verify each — fix only if broken)

| # | Area | Expected behavior | Likely files | Prior task |
|---|------|-------------------|--------------|------------|
| 1 | **Spectra panel collapse** | Below `lg`: FAB or drawer opens full Spectra table; not permanently hidden | `unit-view-client.tsx`, `spectralink-device-table.tsx` | 008 §9 |
| 2 | **Map horizontal stretch** | Patient grid/map fills available width; cards stretch sensibly in columns | `patient-grid.tsx`, `unit-view-client.tsx` | 004, local edits |
| 3 | **Admin tools routing** | Entity/Facility Admin sees admin menu; routes to admin dashboard with facility settings | `auth-container.tsx`, `app-header.tsx`, `admin-dashboard.tsx` | 013, 008 |
| 4 | **Facility dashboard** | User dashboard facility stats + unit entry; admin facility profile section reachable | `user-dashboard.tsx`, `admin-dashboard.tsx` | 013 |
| 5 | **Staff nurse context menu** | Assign/remove staff actions work on nurse cards | `nurse-assignment-card.tsx`, `charge-nurse-card.tsx` | 013 |
| 6 | **Nurse assignment icon bug** | Assignment/slot icons reflect assigned vs empty state correctly (no wrong icon when assigned) | `nurse-assignment-card.tsx`, `nurseHelpers.ts`, `nurse-assignment-sync.ts` | Session fix |
| 7 | **Nurse card resize handle** | Drag handle adjusts patient slot capacity within min/max; persists on save layout | `nurse-card-resize-handle.tsx`, `nurse-card-layout.ts`, nurse cards | Session fix |
| 8 | **Unassigned staff assign button** | `+ Assign staff member` on all unassigned role cards | role card components | 007 |

## Files likely touched (only if fixes required)

- `renderer/src/components/unit-view-client.tsx`
- `renderer/src/components/patient-grid.tsx`
- `renderer/src/components/spectralink-device-table.tsx`
- `renderer/src/components/app-header.tsx`
- `renderer/src/components/admin-dashboard.tsx`
- `renderer/src/components/user-dashboard.tsx`
- `renderer/src/components/nurse-assignment-card.tsx`
- `renderer/src/components/charge-nurse-card.tsx`
- `renderer/src/components/nurse-card-resize-handle.tsx`
- `renderer/src/lib/nurse-card-layout.ts`
- `renderer/src/lib/nurse-assignment-sync.ts`
- `renderer/src/services/nurseHelpers.ts`

## Test plan

For each checklist row:

1. Manual repro steps (document in report).
2. Mark **PASS** / **FAIL** / **PARTIAL**.
3. If FAIL: minimal fix + regression note.
4. Cross-check at desktop (`≥ lg`) and narrow viewport for Spectra (#1).
5. `cd renderer && npx tsc --noEmit` and root `npm run build` after any fixes.

## Acceptance criteria

1. Report table covers all 8 rows with PASS or documented fix.
2. No regressions to WALLDISPLAY privacy or print flows.
3. Build green at **5.1.5-c** after any changes.
4. Completion report: `docs/agents/reports/TASK-20260620-003-DEV01-to-PM01.md` — verification matrix, not a re-implementation log.

## PM note to DEV

If all items PASS, report is still required — PM uses it to close session items without duplicate tasks. Do not duplicate `TASK-20260430-008` work unless verification finds regressions.
