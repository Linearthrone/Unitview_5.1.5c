# TASK-20260422-018 — PM01-to-DEV01

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260422-018 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P2 |
| **Depends on** | None |
| **Related issue** | `ISSUE-20260422-003-unit-map-mouse-wheel-zoom.md` |

## Objective

Implement mouse-wheel zoom for the unit map with stable interaction behavior.

## Scope

Likely touchpoints (adjust as needed):
- `renderer/src/components/patient-grid.tsx`
- `renderer/src/components/unit-view-client.tsx`
- shared UI/container styles for map viewport

## Functional requirements

1. Wheel zoom on unit map viewport:
   - zoom in on wheel-up
   - zoom out on wheel-down
2. Boundaries:
   - min zoom and max zoom limits
3. Default:
   - initial zoom at 100%
4. Reset:
   - provide a quick reset to 100% (button or equivalent control)
5. Interaction safety:
   - drag/drop of room/staff cards still works at non-100% zoom
   - selection and assignment interactions remain correct

## Acceptance criteria

1. Mouse-wheel zoom works reliably in the unit map area.
2. No regressions to card drag/drop behavior.
3. `cd renderer && npx tsc --noEmit` passes.
4. `npm run build` at root passes.

## Completion report

Create: `docs/agents/reports/TASK-20260422-018-DEV01-to-PM01.md`

Include:
- Zoom range used
- How reset is triggered
- Validation notes for drag/drop under zoom
