# TASK-20260620-001 — PM01-to-DEV01

## PM CONTROL STATUS

**STATUS: COMPLETE (DEV report accepted 2026-06-20; pending QA in 007 re-run)**

> **PM note (2026-06-20):** Facility profile, logo on login/prints, light-theme inputs, ReportSheet staff assignments, and prior-shift snapshot on save/activate are implemented; build green. DEV: run checklist, file completion report, queue QA on 003 overlap items.

User requested formal DEV routing (not inline chat fixes). Partial implementation exists in untracked renderer work — DEV must **verify, complete wiring, and deliver green build**.

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260620-001 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P1 |
| **Version** | 5.1.5-c |
| **Related** | `ISSUE-20260430-003-theme-text-contrast-light-dark.md`, `TASK-20260430-005`, `TASK-20260422-013` §1.1.1 |
| **Supersedes partial work in** | Untracked renderer files (facility profile, print header, report sheet staff section) |

## Summary

Complete **facility admin profile + logo branding**, **light-theme input readability**, and **patient ReportSheet staff assignments with prior-shift snapshot persistence**. Foundation types and components exist; wiring and persistence are incomplete.

## Scope checklist

| # | Deliverable | Status (DEV verify) |
|---|-------------|---------------------|
| 1 | Admin panel **Facility Profile** section (name, address, phone, logo upload/remove) | Partial — `facility-settings-form.tsx` in admin dashboard |
| 2 | Persist profile in `SimpleDatabase` (`facility_profile`) | Partial — `database-simple.ts` + `facilityService.ts` |
| 3 | **Login screen** shows saved facility name + logo (not placeholder only) | **Not wired** — `auth-container.tsx` / `login-screen.tsx` |
| 4 | **Charge report** print uses `FacilityPrintHeader` with saved profile | Partial — `printable-report.tsx`; confirm profile loaded in `unit-view-client.tsx` |
| 5 | **Assignment printouts** use `FacilityPrintHeader` with saved profile | Partial — `printable-assignments.tsx`; confirm profile passed at print/preview |
| 6 | **Light theme**: darker input/textarea/select text on light surfaces | Partial — `globals.css` `.theme-light input`; verify all forms/dialogs |
| 7 | **ReportSheet**: current nurse + current PCT (live from cards) | Partial — `report-sheet.tsx` + `patient-staff-assignments.ts` |
| 8 | **ReportSheet**: prior shift nurse + prior shift PCT | UI present; data empty until snapshot wired |
| 9 | **Prior-shift snapshot** on **Save assignments** | **Not wired** — call `snapshotPriorShiftStaff()` before archive |
| 10 | **Prior-shift snapshot** on **Activate oncoming shift** | **Not wired** — snapshot outgoing shift before promote |
| 11 | Persist `priorShiftNurse` / `priorShiftTech` on patient records in DB | Types exist (`patient.ts`); ensure save path updates patients |

## Files likely touched

- `renderer/src/types/facility.ts`
- `renderer/src/types/patient.ts`
- `renderer/src/lib/database-simple.ts`
- `renderer/src/services/facilityService.ts`
- `renderer/src/services/assignmentService.ts`
- `renderer/src/services/patientService.ts`
- `renderer/src/services/nurseService.ts`
- `renderer/src/lib/patient-staff-assignments.ts`
- `renderer/src/components/facility-settings-form.tsx`
- `renderer/src/components/facility-print-header.tsx`
- `renderer/src/components/admin-dashboard.tsx`
- `renderer/src/components/auth-container.tsx`
- `renderer/src/components/login-screen.tsx`
- `renderer/src/components/unit-view-client.tsx`
- `renderer/src/components/printable-report.tsx`
- `renderer/src/components/printable-assignments.tsx`
- `renderer/src/components/report-sheet.tsx`
- `renderer/src/globals.css`

## Test plan

1. Admin → Facility Profile: set name, address, upload PNG logo, save, reload app — values persist.
2. Logout → login screen shows facility name and uploaded logo (not dashed placeholder when logo set).
3. Unit view → print charge report — header shows facility name, address, logo.
4. Unit view → print assignments (and layout preview) — same branding in header.
5. Light theme (`theme-light`): type in login, admit dialog, admin facility form — text clearly readable (not washed out).
6. Assign nurse + PCT to occupied rooms → open ReportSheet → **Current nurse/PCT** match live cards.
7. Save assignments → reopen ReportSheet → **Prior shift nurse/PCT** show names from step 6; current may change after reassignment.
8. Setup oncoming shift → edit board → activate oncoming → prior-shift fields reflect outgoing assignments on patient records.
9. `cd renderer && npx tsc --noEmit` and root `npm run build` — PASS.

## Acceptance criteria

1. Facility profile editable in admin and reflected on login + both print paths.
2. Light-theme form inputs meet contrast expectation (coordinate with or close `TASK-20260430-005` — no conflicting token regressions).
3. ReportSheet shows current and prior shift nurse/PCT with real data after save/activate flows.
4. Prior-shift fields persist across reload for affected patients.
5. Typecheck + build green at **5.1.5-c**.
6. Completion report: `docs/agents/reports/TASK-20260620-001-DEV01-to-PM01.md` with wiring checklist and before/after notes.

## PM note to DEV

Do **not** re-implement from scratch. Audit existing partial code first; list what was already done vs what you wired. If `TASK-20260430-005` contrast work overlaps, merge into this pass and note in both reports.
