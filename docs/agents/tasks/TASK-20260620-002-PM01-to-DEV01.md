# TASK-20260620-002 — PM01-to-DEV01

## PM CONTROL STATUS

**STATUS: COMPLETE (DEV report accepted 2026-06-20; pending QA in 007 re-run)**

> **PM note (2026-06-20):** `authService.initializeAuth()` updated to merge missing default users and sync demo passwords. DEV: confirm login with `wall` / `wall123`, file completion report, queue QA. Do not re-implement unless verification fails.

User-reported: **`wall` / `wall123` invalid credentials** for WALLDISPLAY role. Demo account exists in seed data but fails on installs with existing `localStorage` data.

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260620-002 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P0 |
| **Version** | 5.1.5-c |
| **Related** | `TASK-20260422-013` (WALLDISPLAY role), `renderer/src/types/auth.ts` |

## Summary

Fix auth/database initialization so demo user **`wall`** with password **`wall123`** and **`appRole: 'WALLDISPLAY'`** works reliably — including for users who already have persisted `unitview_data` in localStorage.

## Root cause (PM hypothesis — DEV confirm)

`authService.initializeAuth()` only seeds `defaultUsers` when `existingUsers.length === 0`. Existing localStorage installs never receive the `wall` user added in 013. Password merge runs every init but login still fails with **Employee number not found** when user record is missing.

Secondary risk: existing `wall` user without `appRole: 'WALLDISPLAY'` may authenticate but not get read-only privacy behavior.

## Scope checklist

| # | Requirement |
|---|-------------|
| 1 | Add **idempotent user migration** on init: merge missing entries from `defaultUsers` into stored users (match by `employeeNumber`) |
| 2 | For matched users, backfill **`appRole`** (and other safe defaults) when missing or stale vs seed |
| 3 | Ensure **`defaultPasswords['wall']`** is always present after init (already attempted — verify not overwritten incorrectly) |
| 4 | Do **not** wipe existing custom users or passwords |
| 5 | Optional: bump a `dataVersion` / migration flag in `database-simple.ts` for future seed merges |
| 6 | Admin demo hints panel continues to list `wall` / `wall123` |

## Files likely touched

- `renderer/src/services/authService.ts` — `initializeAuth()` migration logic
- `renderer/src/lib/database-simple.ts` — optional version field + load migration hook
- `renderer/src/types/auth.ts` — reference seed (`defaultUsers`, `defaultPasswords`)
- `renderer/src/components/admin-dashboard.tsx` — demo credentials display (verify only)

## Test plan

1. **Fresh install**: clear `localStorage` → login `wall` / `wall123` → success, lands on user dashboard / unit entry.
2. **Stale install (primary repro)**: with existing `unitview_data` that predates `wall` user → reload app → login `wall` / `wall123` → success (no "Employee number not found").
3. WALLDISPLAY capabilities: unit view read-only, no patient identifiers on cards/report sheet, no admit/edit context menus.
4. Regression: `admin`/`password`, `1001`/`nurse123`, `1002`/`tech123` still work.
5. Custom admin-created users unchanged after migration.
6. `cd renderer && npx tsc --noEmit` and root `npm run build` — PASS.

## Acceptance criteria

1. `wall` / `wall123` login succeeds on fresh and existing localStorage data.
2. Authenticated user has `appRole: 'WALLDISPLAY'` and privacy gating per `lib/roles.ts`.
3. No data loss for existing users/units/patients.
4. Completion report: `docs/agents/reports/TASK-20260620-002-DEV01-to-PM01.md` with migration algorithm summary and repro steps validated.

## PM note to DEV

This is a **one-time migration** pattern users will hit again when new demo accounts ship — prefer a reusable merge helper over hardcoding `wall` only.
