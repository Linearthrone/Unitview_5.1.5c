# TASK-20260430-004 — PM01-to-DEV01

## PM CONTROL STATUS

**STATUS: ACTIVE (DEV EXECUTE NOW — Lane C, P2)**

Supersedes UX details from archived `TASK-20260422-018` (wheel zoom). Keep drag/drop safety requirements.

| Field | Value |
|--------|--------|
| **Task ID** | TASK-20260430-004 |
| **From** | PM-01 |
| **To** | DEV-01 |
| **Priority** | P2 |
| **Related issue** | `ISSUE-20260430-002-unit-map-zoom-controls-and-fit-default.md` |

## Objective

Replace wheel-centric zoom with **+ / − magnifying-glass controls** and **fit-to-view default** per unit.

## Requirements

1. **Controls**
   - Zoom out button with magnifying-glass-minus idiom (`ZoomOut` or equivalent).
   - Zoom in button with magnifying-glass-plus idiom (`ZoomIn` or equivalent).
   - Percentage display between buttons; click resets to fit-to-view.

2. **Default behavior**
   - On unit load / unit switch, zoom auto-calculates to show **entire unit** in viewport.
   - Recalculate on viewport resize when user has not manually adjusted zoom.

3. **Interaction safety**
   - Drag/drop, selection, and nurse-slot drops still work at non-default zoom levels.

4. **Cleanup**
   - Remove or disable wheel zoom if it conflicts with scroll.
   - Review any in-progress local edits in `patient-grid.tsx` and deliver one coherent implementation.

## Acceptance criteria

1. Opening any unit shows full map without manual zoom.
2. +/- controls work with clear magnifying-glass affordance.
3. Typecheck + root build pass.
4. DEV report includes zoom range, fit algorithm summary, drag/drop validation notes.

## Completion report

`docs/agents/reports/TASK-20260430-004-DEV01-to-PM01.md`
